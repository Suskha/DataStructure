# Binary Tree

#### the main file of the program is Main.java

Functions are:

```
insert(Integer data);
inTravese();
findMax();
findMin();
delete(Integer data);
find (Integer data);

```

Functions description:

insert(Integer data) - inserts the data

find(Integer data) - checks whether data is present or not

delete(Integer data) - delete the data

findMax() - finds the maximum value in the tree

findMin() - finds the minimum value in the tree

inTravese() - prints the data in ascending order